We export the custom fonts used inside Odoo to share the same set of icons and ensure looks consistency.

Run `npm run update-icons -- <branch>` to sync the fonts with Odoo. `<branch>` is the Odoo branch to fetch the font from, and defaults to `master`.
