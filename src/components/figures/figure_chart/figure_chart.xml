<templates>
  <t t-name="o-spreadsheet-ChartFigure">
    <div class="o-chart-container w-100 h-100" t-on-dblclick="onDoubleClick">
      <t
        t-component="chartComponent"
        chartId="chartId"
        t-key="chartId"
        isFullScreen="props.isFullScreen"
      />
    </div>
    <div class="o-figure-menu position-absolute m-2" t-if="!env.isDashboard()">
      <div
        class="o-figure-info-icon d-flex flex-row align-items-center align-middle"
        t-if="getAnnotationText() or getAnnotationLink()"
        t-on-click="showInfo"
        t-ref="infoButton">
        <t t-call="o-spreadsheet-Icon.CIRCLE_INFO"/>
      </div>
      <InfoPopover
        t-if="infoState.isOpen"
        anchorRect="infoState.anchorRect"
        onClose="() => this.infoState.isOpen=false"
        annotationText="getAnnotationText()"
        annotationLink="getAnnotationLink()"
      />
    </div>
    <div t-if="env.isDashboard()" class="position-absolute top-0 end-0">
      <ChartDashboardMenu chartId="chartId"/>
    </div>
  </t>
</templates>
