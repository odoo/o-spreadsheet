<templates>
  <t t-name="o-spreadsheet-mobile-Highlight">
    <div class="o-highlight" t-ref="highlight">
      <!-- // remettre quand on aura le env.isMobile -->
      <!-- <t t-foreach="['n', 's', 'w', 'e']" t-as="orientation" t-key="orientation">
        <Border
          onMoveHighlight="(x, y) => this.onMoveHighlight(x,y)"
          isMoving='highlightState.shiftingMode === "isMoving"'
          orientation="orientation"
          zone="props.zone"
        />
      </t> -->
      <t t-foreach="cornerOrientations" t-as="orientation" t-key="orientation">
        <Corner
          onResizeHighlight="(dirX, dirY) => this.onResizeHighlight(dirX, dirY)"
          isResizing='highlightState.shiftingMode === "isResizing"'
          orientation="orientation"
          zone="props.zone"
          color="props.color"
        />
      </t>
    </div>
  </t>
</templates>
