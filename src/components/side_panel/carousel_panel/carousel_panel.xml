<templates>
  <t t-name="o-spreadsheet-CarouselPanel">
    <div class="o-carousel-panel h-100 overflow-auto">
      <Section title.translate="Carousel Title" class="'o-carousel-title'">
        <TextInput
          value="title?.text ?? ''"
          onChange.bind="updateTitleText"
          placeholder.translate="Add a Title"
          alwaysShowBorder="true"
          selectContentOnFocus="true"
        />
        <TextStyler
          style="title ?? {}"
          updateStyle.bind="updateTitleStyle"
          defaultStyle="DEFAULT_CAROUSEL_TITLE_STYLE"
          hasHorizontalAlign="false"
        />
      </Section>
      <Section title.translate="Layout">
        <div class="d-flex gap-2">
          <div
            class="o-button px-3 py-1"
            t-att-class="{'o-button-active': layout === 'tabs'}"
            t-on-click="() => this.setLayout('tabs')">
            Tabs
          </div>
          <div
            class="o-button px-3 py-1"
            t-att-class="{'o-button-active': layout === 'row'}"
            t-on-click="() => this.setLayout('row')">
            Row
          </div>
          <div
            class="o-button px-3 py-1"
            t-att-class="{'o-button-active': layout === 'grid'}"
            t-on-click="() => this.setLayout('grid')">
            Grid
          </div>
        </div>
        <t t-if="layout === 'grid'">
          <div class="d-flex align-items-center gap-2 mt-2">
            <span>Columns:</span>
            <input
              type="number"
              class="o-input"
              style="width: 60px;"
              t-att-value="gridColumns"
              min="1"
              max="12"
              t-on-change="(ev) => this.setGridColumns(parseInt(ev.target.value))"
            />
          </div>
        </t>
      </Section>
      <div class="o-section pb-1 pt-0">
        <div class="o-section-title">Carousel Sections</div>
      </div>
      <div class="o-carousel-preview-list overflow-auto bg-white" t-ref="previewList">
        <t t-foreach="carouselItems" t-as="item" t-key="getItemId(item)">
          <div
            class="o-carousel-preview border-bottom pe-2 position-relative w-100 d-flex align-items-center"
            t-att-class="{
              'o-dragging': dragAndDrop.draggedItemId === getItemId(item),
              'o-selected': isCarouselItemActive(item)
            }"
            t-att-style="getPreviewDivStyle(item)">
            <div
              class="o-drag-handle h-100 position-absolute d-flex align-items-center o-button-icon ps-1 flex-shrink-0"
              t-on-pointerdown.stop.prevent="(ev) => this.onDragHandleMouseDown(item, ev)">
              <t t-call="o-spreadsheet-Icon.THIN_DRAG_HANDLE"/>
            </div>
            <div
              class="o-carousel-preview-icon ms-3 flex-shrink-0 d-flex align-items-center justify-content-center">
              <t t-call="{{getItemPreview(item)}}"/>
            </div>
            <div class="o-carousel-preview-title text-truncate ms-2">
              <TextInput
                value="getItemTitle(item)"
                onChange="(newName) => this.renameCarouselItem(item, newName)"
                selectContentOnFocus="true"
              />
            </div>
            <div class="ms-auto"/>
            <t t-if="layout === 'grid'">
              <div class="d-flex align-items-center gap-1 flex-shrink-0 ms-1">
                <input
                  type="number"
                  class="o-input o-grid-span-input"
                  title="Column span"
                  t-att-value="item.colSpan ?? 1"
                  min="1"
                  t-att-max="gridColumns"
                  t-on-change="(ev) => this.setItemColSpan(item, parseInt(ev.target.value))"
                />
                <span>x</span>
                <input
                  type="number"
                  class="o-input o-grid-span-input"
                  title="Row span"
                  t-att-value="item.rowSpan ?? 1"
                  min="1"
                  t-on-change="(ev) => this.setItemRowSpan(item, parseInt(ev.target.value))"
                />
              </div>
            </t>
            <div class="flex-shrink-0 d-flex me-1">
              <CogWheelMenu items="getCogWheelMenuItems(item)"/>
            </div>
          </div>
        </t>
      </div>
      <t t-if="activeDataLayerItem">
        <Section title.translate="Data Range">
          <SelectionInput
            t-key="activeDataLayerItem.id"
            ranges="[getDataLayerRangeString(activeDataLayerItem)]"
            hasSingleRange="true"
            onSelectionChanged="(ranges) => this.onDataLayerRangeChanged(ranges)"
            onSelectionConfirmed.bind="onDataLayerRangeConfirmed"
          />
        </Section>
      </t>
      <div
        class="o-button-link o-carousel-add-chart float-end d-flex align-items-center py-4 pe-4 gap-2"
        t-on-click="addNewChartToCarousel"
        t-att-title="carouselAddChartInfoMessage">
        + Add chart
      </div>
      <div class="o-section px-4 py-2 d-flex align-items-center gap-2">
        <input type="checkbox" t-att-checked="showDataView" t-on-change="toggleShowDataView"/>
        <span t-att-title="carouselDataViewMessage">Show data view</span>
      </div>
      <div
        class="o-button-link o-carousel-add-data-layer float-end py-4 pe-4"
        t-on-click="onAddDataLayer">
        + Add data layer
      </div>
    </div>
  </t>
</templates>
