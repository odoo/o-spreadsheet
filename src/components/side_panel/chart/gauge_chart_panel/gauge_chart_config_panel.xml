<templates>
  <t t-name="o-spreadsheet-GaugeChartConfigPanel" owl="1">
    <div>
      <div class="o-section o-data-series p-4">
        <div class="o-section-title mb-2 fw-bold text-secondary">Data range</div>
        <SelectionInput
          ranges="[props.definition.dataRange || '']"
          isInvalid="isDataRangeInvalid"
          hasSingleRange="true"
          required="true"
          onSelectionChanged="(ranges) => this.onDataRangeChanged(ranges)"
          onSelectionConfirmed="() => this.updateDataRange()"
        />
      </div>
      <div
        class="o-section o-sidepanel-error text-danger mt-2 p-4"
        t-if="configurationErrorMessages">
        <div t-foreach="configurationErrorMessages" t-as="error" t-key="error">
          <t t-esc="error"/>
        </div>
      </div>
    </div>
  </t>
</templates>
