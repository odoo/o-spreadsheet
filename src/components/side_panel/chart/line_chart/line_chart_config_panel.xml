<templates>
  <t t-name="o-spreadsheet-LineConfigPanel">
    <div>
      <Section className="'pt-0'">
        <Checkbox
          name="'stacked'"
          value="props.definition.stacked"
          update.bind="onUpdateStacked"
          label="'Stacked linechart'"
        />
        <Checkbox
          name="'cumulative'"
          value="props.definition.cumulative"
          update.bind="onUpdateCumulative"
          label="'Cumulative data'"
        />
      </Section>
      <Section title="'Data Series'" className="'o-data-series'">
        <SelectionInput
          ranges="() => this.getDataSeriesRanges()"
          required="true"
          onSelectionChanged="(ranges) => this.onDataSeriesRangesChanged(ranges)"
          onSelectionConfirmed="() => this.onDataSeriesConfirmed()"
        />
      </Section>
      <Section title="'Categories / Labels'" className="'o-data-labels'">
        <SelectionInput
          ranges="() => [this.getLabelRange()]"
          isInvalid="isLabelInvalid"
          hasSingleRange="true"
          onSelectionChanged="(ranges) => this.onLabelRangeChanged(ranges)"
          onSelectionConfirmed="() => this.onLabelRangeConfirmed()"
        />
        <Checkbox
          name="'aggregated'"
          value="props.definition.aggregated"
          update.bind="onUpdateAggregated"
          label="'Aggregate'"
        />
        <Checkbox
          t-if="canTreatLabelsAsText"
          name="'labelsAsText'"
          value="props.definition.labelsAsText"
          update.bind="onUpdateLabelsAsText"
          label="'Treat labels as text'"
        />
      </Section>
      <Section className="'o-use-row-as-headers'" t-if="calculateHeaderPosition()">
        <Checkbox
          name="'dataSetsHaveTitle'"
          value="props.definition.dataSetsHaveTitle"
          update.bind="onUpdateDataSetsHaveTitle"
          label="'Use row ' + calculateHeaderPosition() + ' as headers'"
        />
      </Section>

      <Section t-if="errorMessages.length">
        <ValidationMessages messages="errorMessages" msgType="'error'"/>
      </Section>
    </div>
  </t>
</templates>
