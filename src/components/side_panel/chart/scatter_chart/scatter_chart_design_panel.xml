<templates>
  <t t-name="o-spreadsheet-ScatterChartDesignPanel">
    <GeneralDesignEditor t-props="props">
      <t t-set-slot="general-extension">
        <ChartLegend t-props="props"/>
        <Section class="'pt-0'" title.translate="Values">
          <ChartShowValues
            t-props="props"
            onModeChanged.bind="onShowValuesModeChanged"
            modes="showValuesModes"
          />
        </Section>
        <Section class="'pt-0'" title.translate="Zoom">
          <Checkbox
            name="'zoomable'"
            label.translate="Show slicer"
            value="props.definition.zoomable"
            onChange.bind="onToggleZoom"
            className="'mb-2'"
          />
        </Section>
      </t>
    </GeneralDesignEditor>
    <SeriesWithAxisDesignEditor t-props="props">
      <t t-set-slot="general-extension" t-slot-scope="scope">
        <t t-set="index" t-value="scope.index"/>
        <Section class="'pt-0 px-0'" title="chartTerms.PointSizesTitle">
          <RadioSelection
            choices="pointSizeModes"
            selectedValue="getPointSizeMode(index)"
            name="'point-size-mode'"
            onChange="(value) => this.onPointSizeModeChanged(index, value)"
          />
          <div class="mt-2" t-if="getPointSizeMode(index) === 'fixed'">
            <span class="o-section-subtitle">
              <t t-esc="chartTerms.PointSizeValueLabel"/>
            </span>
            <input
              class="o-input w-100"
              type="number"
              t-att-min="pointSizeMin"
              t-att-max="pointSizeMax"
              t-att-value="getPointSizeValue(index)"
              t-on-change="(ev) => this.onPointSizeValueChanged(index, ev)"
            />
          </div>
          <div
            class="o-text-muted mt-2"
            t-if="getPointSizeMode(index) === 'range' and !hasPointSizeRange(index)">
            <t t-esc="chartTerms.PointSizeRangeHelper"/>
          </div>
        </Section>
      </t>
    </SeriesWithAxisDesignEditor>
    <SidePanelCollapsible isInitiallyCollapsed="true" title.translate="Axes">
      <t t-set-slot="content">
        <AxisDesignEditor
          axesList="axesList"
          chartId="props.chartId"
          definition="props.definition"
          updateChart="props.updateChart"
        />
      </t>
    </SidePanelCollapsible>
  </t>
</templates>
